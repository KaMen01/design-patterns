在不破坏封装性的前提下，捕获一个对象的内部状态，并在该对象之外保存这个状态。
这样以后就可以将该对象恢复到原先保存的状态。
主要思考点：
    如何做到将对象的状态保存在外部，但是外部却仍旧不知道对象的属性细节
解决思路：
    火车箱包寄存处，我们将自己的东西放入了保重，存入寄存处，对外不暴露箱子中的东西
三个角色
    Originator 原发器角色
    Memento 备忘录角色
    CareTaker 备忘录管理员角色
